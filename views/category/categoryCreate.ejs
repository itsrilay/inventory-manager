<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <h1><%= title %></h1>
    <%- include("../partials/errors.ejs") %>
    <form
      method="POST"
      action="<%= category.id ? `/categories/${category.id}/update` : '/categories/create' %>">
      <label for="name">Name</label>
      <input
        type="text"
        name="name"
        id="name"
        value="<%= category.name ? category.name : '' %>" />
      <button type="submit">Submit</button>
    </form>
  </body>
</html>
